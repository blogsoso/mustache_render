<h1>新建模板文件</h1>

<% form_for(@mustache_render_template, :url => mustache_render_manager_folder_templates_url(
  :folder_id => @mustache_render_template.folder_id
)) do |f| %>

<%= render :partial => 'form', :locals => { :f => f } %>

<% end %>

<%= link_to "返回上一级", mustache_render_manager_folder_url(:id => @mustache_render_template.folder_id) %>

