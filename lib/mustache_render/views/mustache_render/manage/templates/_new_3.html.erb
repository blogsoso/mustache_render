
  <%= form_for(@mustache_render_template, :url => mustache_render_manage_folder_templates_url(
    :folder_id => @mustache_render_template.folder_id
  )) do |f| %>
  
  <%= render :partial => 'form', :locals => { :f => f } %>
  
  <% end %>

